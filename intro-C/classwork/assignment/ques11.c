#include <stdio.h>
int main()
{
  // instead of creating multiple integer variable we can just  create an integer array
  int i, array[] = {1, 2, 3, 4, 5};

  for (i = 0; i < 5; i++)
  {
    printf("%d", array[i]);
  };
  // when i fly towards u
  return 0;
}