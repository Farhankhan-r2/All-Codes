#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
  float x = 45;
  // x= round(x); rounding a no
  // x = ciel(x); round up
  // x = floor(x); round down
  // x = abs(x);
  // x = log(x);
  x = sin(x);
  printf("%f", x);

  // int x = 9;
  // x = sqrt(x);
  // x = pow(x,2);
  // printf("%d", x);

  return 0;
}